import QtQuick 2.3
import QtQuick.Window 2.2
import QtAudioEngine 1.0

Window {
    objectName: "Main"
    visible: true
    width: 100
    height: 100
    Rectangle {
        color: "red"
        x: 300
        y: 200
    }


    /*
    Scene {

        //listener: Qt.point(theListener.x, theListener.y)
    }
    */
}
